# Admin Mini App

Админ-панель для бота LOOV с **полностью автоматической** интеграцией Cloudflare Tunnel.

## 🚀 Быстрый старт

### 1. Установите cloudflared на VPS

**Ubuntu/Debian:**
```bash
curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb -o cloudflared.deb
sudo dpkg -i cloudflared.deb
```

**macOS:**
```bash
brew install cloudflared
```

### 2. Добавьте свой Telegram ID в список админов

Отредактируйте `app/admin/security.py`:
```python
ADMIN_WHITELIST: set[int] = {ВАШ_TELEGRAM_ID}
```
*(Узнать ID можно через бота @userinfobot)*

### 3. Запустите бота

```bash
python manage.py run
```

### 4. Готово!

При запуске бота вы увидите в логах:
```
🔐 Admin панель доступна: https://xxx-xxx-xxx.trycloudflare.com/admin
```

Отправьте `/admin` боту в Telegram — появится кнопка для открытия панели.

**Никаких дополнительных действий не требуется!** URL туннеля автоматически передаётся в кнопку WebApp.

## Как это работает

```
┌─────────────────┐     ┌─────────────────────────────┐     ┌───────────┐
│   Telegram      │────▶│  Cloudflare Tunnel (HTTPS)  │────▶│ VPS:8080  │
│   WebApp        │     │  xxx.trycloudflare.com      │     │ Admin API │
│                 │     │                             │     │           │
│   /admin ───────┼─────┼─► Frontend + API            │     │ Frontend  │
│                 │     │   (same origin)             │     │ + API     │
└─────────────────┘     └─────────────────────────────┘     └───────────┘
```

1. **Бот запускается** → запускает Admin API на порту 8080
2. **Cloudflare Tunnel стартует автоматически** → создаёт HTTPS URL
3. **URL туннеля сохраняется** в роутере бота
4. **При команде `/admin`** → бот показывает кнопку с актуальным URL туннеля
5. **Telegram открывает WebApp** → фронтенд и API на одном домене (туннеля)

## Конфигурация

### Переменные окружения (опционально)

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `ADMIN_API_HOST` | Хост для API сервера | `0.0.0.0` |
| `ADMIN_API_PORT` | Порт для API сервера | `8080` |

> **Примечание:** `ADMIN_WEBAPP_URL` в `.env` больше не обязателен — URL генерируется автоматически.

### Список админов

Редактируйте `app/admin/security.py`:
```python
ADMIN_WHITELIST: set[int] = {123456789, 987654321}
```

## Эндпоинты API

| Эндпоинт | Описание |
|----------|----------|
| `GET /admin` | UI админ-панели |
| `GET /admin/api/users` | API данных (требует `X-Telegram-Init-Data`) |

## Безопасность

- Команда `/admin` не показывается в меню бота
- Не-админы не получают ответа на `/admin`
- API требует валидный Telegram `initData`
- CORS включён для совместимости с Telegram WebApp

## Решение проблем

### "Init data missing" / "Open inside Telegram"
- Админ-панель должна открываться **только через Telegram** (кнопка WebApp)
- Открытие URL в обычном браузере не работает — это нормально

### Нет ответа на команду `/admin`
- Добавьте свой Telegram ID в `ADMIN_WHITELIST` в `app/admin/security.py`

### "Admin panel URL is not configured"
- Проверьте, что cloudflared установлен: `cloudflared --version`
- Проверьте логи бота — должна быть строка `TUNNEL_STARTED`

### "Unable to load data" / Сервер недоступен
1. Проверьте, что бот запущен
2. Проверьте логи на наличие ошибок туннеля
3. Попробуйте перезапустить бота

### Пустой список пользователей
- База данных может быть пуста, если никто ещё не использовал бота
